import React, { type ReactElement, type MutableRefObject } from "react";

const SelectRestaurant = (): ReactElement => {

    return (
       <h1>Select Restaurant</h1>
    );
  };
  
  export default SelectRestaurant;